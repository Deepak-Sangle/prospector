import type { App } from '@slack/bolt';

/**
 * Register view listeners with the Bolt app. No modal views yet — the monitor
 * creation modal will register here.
 */
export function register(_app: App): void {}
